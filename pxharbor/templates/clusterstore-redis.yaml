apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: {{ .Values.clusterSecretStore1.name }}
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  provider:
    kubernetes:
      remoteNamespace: {{ .Values.clusterSecretStore1.remoteNamespace }}
      server:
        url: "https://kubernetes.default"
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
          namespace: default
      auth:
        serviceAccount:
          name: {{ .Values.clusterSecretStore1.serviceAccountName }}
          namespace: {{ .Values.clusterSecretStore1.serviceAccountNamespace }}
